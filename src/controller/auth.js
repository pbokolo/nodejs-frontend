class Controller {
  #dispatch;
  initialState = { email: "", password: "", repassword: "" };
  constructor(dispatch) {
    this.dispatch = dispatch;
  }

  signup(creds) {}
  signup(creds) {}
}

export { Controller };
